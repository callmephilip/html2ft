{
  "name": "html2ft-monorepo",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.12.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/callmephilip/html2ft.git"
  },
  "keywords": [
    "fasthtml",
    "ft",
    "vscode-extension"
  ],
  "scripts": {
    "test": "pnpm -r --parallel --filter='./packages/*' run test"
  }
}
